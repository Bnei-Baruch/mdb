// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/randomize"
	"github.com/volatiletech/sqlboiler/strmangle"
)

func testTwitterUsers(t *testing.T) {
	t.Parallel()

	query := TwitterUsers(nil)

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}
func testTwitterUsersDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = twitterUser.Delete(tx); err != nil {
		t.Error(err)
	}

	count, err := TwitterUsers(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTwitterUsersQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = TwitterUsers(tx).DeleteAll(); err != nil {
		t.Error(err)
	}

	count, err := TwitterUsers(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTwitterUsersSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	slice := TwitterUserSlice{twitterUser}

	if err = slice.DeleteAll(tx); err != nil {
		t.Error(err)
	}

	count, err := TwitterUsers(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}
func testTwitterUsersExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	e, err := TwitterUserExists(tx, twitterUser.ID)
	if err != nil {
		t.Errorf("Unable to check if TwitterUser exists: %s", err)
	}
	if !e {
		t.Errorf("Expected TwitterUserExistsG to return true, but got false.")
	}
}
func testTwitterUsersFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	twitterUserFound, err := FindTwitterUser(tx, twitterUser.ID)
	if err != nil {
		t.Error(err)
	}

	if twitterUserFound == nil {
		t.Error("want a record, got nil")
	}
}
func testTwitterUsersBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = TwitterUsers(tx).Bind(twitterUser); err != nil {
		t.Error(err)
	}
}

func testTwitterUsersOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	if x, err := TwitterUsers(tx).One(); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testTwitterUsersAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUserOne := &TwitterUser{}
	twitterUserTwo := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUserOne, twitterUserDBTypes, false, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}
	if err = randomize.Struct(seed, twitterUserTwo, twitterUserDBTypes, false, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUserOne.Insert(tx); err != nil {
		t.Error(err)
	}
	if err = twitterUserTwo.Insert(tx); err != nil {
		t.Error(err)
	}

	slice, err := TwitterUsers(tx).All()
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testTwitterUsersCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	twitterUserOne := &TwitterUser{}
	twitterUserTwo := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUserOne, twitterUserDBTypes, false, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}
	if err = randomize.Struct(seed, twitterUserTwo, twitterUserDBTypes, false, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUserOne.Insert(tx); err != nil {
		t.Error(err)
	}
	if err = twitterUserTwo.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := TwitterUsers(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testTwitterUsersInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := TwitterUsers(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTwitterUsersInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx, twitterUserColumnsWithoutDefault...); err != nil {
		t.Error(err)
	}

	count, err := TwitterUsers(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTwitterUserToManyUserTwitterTweets(t *testing.T) {
	var err error
	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	var a TwitterUser
	var b, c TwitterTweet

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	if err := a.Insert(tx); err != nil {
		t.Fatal(err)
	}

	randomize.Struct(seed, &b, twitterTweetDBTypes, false, twitterTweetColumnsWithDefault...)
	randomize.Struct(seed, &c, twitterTweetDBTypes, false, twitterTweetColumnsWithDefault...)

	b.UserID = a.ID
	c.UserID = a.ID
	if err = b.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx); err != nil {
		t.Fatal(err)
	}

	twitterTweet, err := a.UserTwitterTweets(tx).All()
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range twitterTweet {
		if v.UserID == b.UserID {
			bFound = true
		}
		if v.UserID == c.UserID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := TwitterUserSlice{&a}
	if err = a.L.LoadUserTwitterTweets(tx, false, (*[]*TwitterUser)(&slice)); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.UserTwitterTweets); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.UserTwitterTweets = nil
	if err = a.L.LoadUserTwitterTweets(tx, true, &a); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.UserTwitterTweets); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", twitterTweet)
	}
}

func testTwitterUserToManyAddOpUserTwitterTweets(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer tx.Rollback()

	var a TwitterUser
	var b, c, d, e TwitterTweet

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, twitterUserDBTypes, false, strmangle.SetComplement(twitterUserPrimaryKeyColumns, twitterUserColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*TwitterTweet{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, twitterTweetDBTypes, false, strmangle.SetComplement(twitterTweetPrimaryKeyColumns, twitterTweetColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*TwitterTweet{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddUserTwitterTweets(tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.ID != first.UserID {
			t.Error("foreign key was wrong value", a.ID, first.UserID)
		}
		if a.ID != second.UserID {
			t.Error("foreign key was wrong value", a.ID, second.UserID)
		}

		if first.R.User != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.User != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.UserTwitterTweets[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.UserTwitterTweets[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.UserTwitterTweets(tx).Count()
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testTwitterUsersReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	if err = twitterUser.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testTwitterUsersReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	slice := TwitterUserSlice{twitterUser}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}
func testTwitterUsersSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	slice, err := TwitterUsers(tx).All()
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	twitterUserDBTypes = map[string]string{`AccountID`: `character varying`, `DisplayName`: `character varying`, `ID`: `bigint`, `Username`: `character varying`}
	_                  = bytes.MinRead
)

func testTwitterUsersUpdate(t *testing.T) {
	t.Parallel()

	if len(twitterUserColumns) == len(twitterUserPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := TwitterUsers(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	if err = twitterUser.Update(tx); err != nil {
		t.Error(err)
	}
}

func testTwitterUsersSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(twitterUserColumns) == len(twitterUserPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	twitterUser := &TwitterUser{}
	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Insert(tx); err != nil {
		t.Error(err)
	}

	count, err := TwitterUsers(tx).Count()
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, twitterUser, twitterUserDBTypes, true, twitterUserPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(twitterUserColumns, twitterUserPrimaryKeyColumns) {
		fields = twitterUserColumns
	} else {
		fields = strmangle.SetComplement(
			twitterUserColumns,
			twitterUserPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(twitterUser))
	updateMap := M{}
	for _, col := range fields {
		updateMap[col] = value.FieldByName(strmangle.TitleCase(col)).Interface()
	}

	slice := TwitterUserSlice{twitterUser}
	if err = slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	}
}
func testTwitterUsersUpsert(t *testing.T) {
	t.Parallel()

	if len(twitterUserColumns) == len(twitterUserPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	twitterUser := TwitterUser{}
	if err = randomize.Struct(seed, &twitterUser, twitterUserDBTypes, true); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer tx.Rollback()
	if err = twitterUser.Upsert(tx, false, nil, nil); err != nil {
		t.Errorf("Unable to upsert TwitterUser: %s", err)
	}

	count, err := TwitterUsers(tx).Count()
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &twitterUser, twitterUserDBTypes, false, twitterUserPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TwitterUser struct: %s", err)
	}

	if err = twitterUser.Upsert(tx, true, nil, nil); err != nil {
		t.Errorf("Unable to upsert TwitterUser: %s", err)
	}

	count, err = TwitterUsers(tx).Count()
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
