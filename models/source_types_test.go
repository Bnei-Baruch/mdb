// Code generated by SQLBoiler 4.8.6 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testSourceTypes(t *testing.T) {
	t.Parallel()

	query := SourceTypes()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testSourceTypesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SourceTypes().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSourceTypesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := SourceTypes().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SourceTypes().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSourceTypesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SourceTypeSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SourceTypes().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSourceTypesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := SourceTypeExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if SourceType exists: %s", err)
	}
	if !e {
		t.Errorf("Expected SourceTypeExists to return true, but got false.")
	}
}

func testSourceTypesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	sourceTypeFound, err := FindSourceType(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if sourceTypeFound == nil {
		t.Error("want a record, got nil")
	}
}

func testSourceTypesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = SourceTypes().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testSourceTypesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := SourceTypes().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testSourceTypesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	sourceTypeOne := &SourceType{}
	sourceTypeTwo := &SourceType{}
	if err = randomize.Struct(seed, sourceTypeOne, sourceTypeDBTypes, false, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}
	if err = randomize.Struct(seed, sourceTypeTwo, sourceTypeDBTypes, false, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = sourceTypeOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = sourceTypeTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SourceTypes().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testSourceTypesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	sourceTypeOne := &SourceType{}
	sourceTypeTwo := &SourceType{}
	if err = randomize.Struct(seed, sourceTypeOne, sourceTypeDBTypes, false, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}
	if err = randomize.Struct(seed, sourceTypeTwo, sourceTypeDBTypes, false, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = sourceTypeOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = sourceTypeTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SourceTypes().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testSourceTypesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SourceTypes().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSourceTypesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(sourceTypeColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := SourceTypes().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSourceTypeToManyTypeSources(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a SourceType
	var b, c Source

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, sourceDBTypes, false, sourceColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, sourceDBTypes, false, sourceColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.TypeID = a.ID
	c.TypeID = a.ID

	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.TypeSources().All(tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.TypeID == b.TypeID {
			bFound = true
		}
		if v.TypeID == c.TypeID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := SourceTypeSlice{&a}
	if err = a.L.LoadTypeSources(tx, false, (*[]*SourceType)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.TypeSources); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.TypeSources = nil
	if err = a.L.LoadTypeSources(tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.TypeSources); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testSourceTypeToManyAddOpTypeSources(t *testing.T) {
	var err error

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()

	var a SourceType
	var b, c, d, e Source

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, sourceTypeDBTypes, false, strmangle.SetComplement(sourceTypePrimaryKeyColumns, sourceTypeColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*Source{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, sourceDBTypes, false, strmangle.SetComplement(sourcePrimaryKeyColumns, sourceColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*Source{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddTypeSources(tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.ID != first.TypeID {
			t.Error("foreign key was wrong value", a.ID, first.TypeID)
		}
		if a.ID != second.TypeID {
			t.Error("foreign key was wrong value", a.ID, second.TypeID)
		}

		if first.R.Type != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Type != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.TypeSources[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.TypeSources[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.TypeSources().Count(tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testSourceTypesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testSourceTypesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SourceTypeSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testSourceTypesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SourceTypes().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	sourceTypeDBTypes = map[string]string{`ID`: `bigint`, `Name`: `character varying`}
	_                 = bytes.MinRead
)

func testSourceTypesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(sourceTypePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(sourceTypeAllColumns) == len(sourceTypePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SourceTypes().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testSourceTypesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(sourceTypeAllColumns) == len(sourceTypePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SourceType{}
	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypeColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SourceTypes().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, sourceTypeDBTypes, true, sourceTypePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(sourceTypeAllColumns, sourceTypePrimaryKeyColumns) {
		fields = sourceTypeAllColumns
	} else {
		fields = strmangle.SetComplement(
			sourceTypeAllColumns,
			sourceTypePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := SourceTypeSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testSourceTypesUpsert(t *testing.T) {
	t.Parallel()

	if len(sourceTypeAllColumns) == len(sourceTypePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := SourceType{}
	if err = randomize.Struct(seed, &o, sourceTypeDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SourceType: %s", err)
	}

	count, err := SourceTypes().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, sourceTypeDBTypes, false, sourceTypePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SourceType struct: %s", err)
	}

	if err = o.Upsert(tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SourceType: %s", err)
	}

	count, err = SourceTypes().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
